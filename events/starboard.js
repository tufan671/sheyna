

module.exports.star = class {
  constructor(client) {
    this.client = client;
  }
  async check(reaction,user) {
    let message = reaction.message;
    let channel = message.channel;
    let guild = channel.guild;
    
  }
}